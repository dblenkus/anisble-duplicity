---

- name: Install system dependencies
  package: name={{ item }} state=present
  with_items:
    - python-pip
    - gnupg2
    - ncftp
    - python-paramiko
    - python-boto
    - python-devel
    - librsync-devel
    - python-lockfile
    - mailx
    - gcc

- name: Create download dir
  file: path={{ duplicity_dist_dir }} state=directory

- name: Download the Duplicity tarball
  get_url:
    url: "{{ duplicity_download_url }}"
    dest: "{{ duplicity_dist_dir }}/{{ duplicity_version }}.tar.gz"
  register: duplicity_download_res

- name: Untar the duplicity tar.gz
  unarchive:
    src: "{{ duplicity_dist_dir }}/{{ duplicity_version }}.tar.gz"
    dest: "{{ duplicity_dist_dir }}"
    copy: no

- name: Install duplicity
  shell: >
    python setup.py install
    chdir={{ duplicity_dist_dir }}/{{ duplicity_version }}
  when: duplicity_download_res.changed

- name: Install duptools script (Duplicity wrapper)
  template:
    src: bin/duptools.j2
    dest: "{{ duplicity_bin_dir }}/duptools"
    mode: 0755

- name: Create file with credentials
  template:
    src: home/duplicity_credentials.j2
    dest: "~/.duplicity_credentials"
    mode: 0700

- name: Create cron job for backups
  cron:
    name: backup
    weekday: "{{ duplicity_cron_weekday }}"
    day: "{{ duplicity_cron_day }}"
    hour: "{{ duplicity_cron_hour }}"
    minute: "{{ duplicity_cron_minute }}"
    job: "{{ duplicity_bin_dir }}/duptools backup"
  when: duplicity_create_cronjob
