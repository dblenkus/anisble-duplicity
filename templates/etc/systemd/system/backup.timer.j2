# {{ ansible_managed }}

[Unit]
Description=Schedule backup of the {{ item.name }}

[Timer]
OnCalendar=*-*-* 4:30:00
Unit=backup-{{ item.name }}.service
Persistent=true

[Install]
WantedBy=timers.target
