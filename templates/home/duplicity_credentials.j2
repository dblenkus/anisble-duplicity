#!/bin/bash
# {{ ansible_managed }}

export AWS_ACCESS_KEY_ID='{{ backup_aws_key_id }}'
export AWS_SECRET_ACCESS_KEY='{{ vaulted_backup_aws_key }}'
export PASSPHRASE='{{ vaulted_backup_passphrase }}'
