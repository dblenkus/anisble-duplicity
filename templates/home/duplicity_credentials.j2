#!/bin/bash
# {{ ansible_managed }}

export AWS_ACCESS_KEY_ID='{{ duplicity_aws_key_id }}'
export AWS_SECRET_ACCESS_KEY='{{ vaulted_duplicity_aws_key }}'
export PASSPHRASE='{{ vaulted_duplicity_passphrase }}'
